# action.yml
name: 'Sync Lark Bitable to Github Issue'
description: 'Create and Update Github Issue from Lark Bitable'
runs:
  using: 'docker'
  image: 'docker://ghcr.io/chronoaiproject/lark-github:latest'
